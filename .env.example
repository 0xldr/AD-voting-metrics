# Copy this file to .env and fill in your own values.
# .env is gitignored and should never be committed.

# Mainnet JSON-RPC endpoint used to fetch on-chain Lock/Free delegation events
# and to verify "Pending verification" executive votes. Any provider works —
# Alchemy / Infura / a public RPC. Required: without it the delegation sync
# fails and no spell vote can be credited.
SKY_RPC_URL=
